machine:
  java:
    version: oraclejdk8
  services:
    - docker
deployment:
  master:
    branch: master
    commands:
      - mvn package
      - docker info
      - docker build -f ./Dockerfile.build -t dmitriid/tetrad .

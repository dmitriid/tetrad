machine:
  java:
    version: oraclejdk8
  services:
    - docker
deployment:
  master:
    branch: master
    commands:
      - mvn package
      - docker build -f ./Dockerfile.run -t dmitriid/tetrad .
